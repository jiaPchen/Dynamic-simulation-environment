# 阶段目标对照与剩余验收

本文件以根目录《项目推进(1).docx》的阶段一、阶段二任务为当前交付范围，并对照“新阶段项目需求”中的五份总体与详细方案。下表记录截至 2026-09-25 在当前电脑上的核查结果；换机后仍须重新预检和确认 TruckSim Run。五份方案还包含共享工况库、统一主模型、后续工程化和 HIL 等更远目标，不应混作《项目推进》当前阶段的必验项。

| 范围 | 当前实现与可核查证据 | 尚需确认或完成 |
| --- | --- | --- |
| 阶段一基础联仿 | `00trucksim_simulink/` 有独立模型、TXT、MATLAB 一键脚本、TruckSim 配置、CSV 与报告。2026-09-25 第 024 次正弦工况已真实复跑；正式 CSV 为 0～12 s、1201 行、0.01 s 采样，报告已生成。 | 当前电脑的本项复跑核对已完成；换机后须重新确认运行环境及专用 Run。 |
| 阶段二 Python 控制闭环 | `01_trucksim_simulink_python/` 有零质心侧偏角多轴转角控制器、TCP 逐步通信、阶跃/正弦 TXT、单工况和批量入口、两份 CSV 与 Word 报告。2026-09-25 12:46 的四工况批次均为 `COMPLETED`，执行检查为 `PASS`；四次日志均包含 `CONFIG_READY`、`HANDSHAKE_OK`、`STOP_OK` 和 `SERVER_STOPPED`，批量指标汇总显示 0.01 s 采样。 | 报告的控制性能状态仍为 `NOT_EVALUATED`。同条件 40 km/h 正弦工况中，阶段一与阶段二记录的最大绝对侧偏角分别约为 0.206° 和 0.152°，可作为本工况的观察结果，不能据此宣称全部工况已达到未定义的性能阈值。 |
| TruckSim 工况配置 | 当前机器的阶段二专用 `simfile` 可关联 `.par`，写入并回读速度、附着、时长等工况参数；常规四工况已完成真实联仿。 | COM 实测、非零坡度、起伏场景及初始/目标车速不同的真实响应验证属于扩展或后续方案范围，不是《项目推进》阶段二的必验项；不能把 `.par` 回读或离线测试写成 COM 实测通过。 |
| 运行安全与追溯 | 阶段二预检检查 Python 依赖、solver、固定 6 输入/27 输出端口基线和 `INPUT` 参数文件；失败工况保留用例、日志和错误堆栈。两个阶段保存专用 `simfile` 时，若另一阶段入口有效则拒绝共用 Run。 | 静态端口基线不等于自动证明全部 Simulink 信号顺序正确；换机后仍须复核模型接口和两个阶段各自的 Run。 |
| 两路线统一架构与共享工况库 | 两个阶段目前可分别复制与运行，使用独立 `.slx` 模型和各自的 TXT 用例。 | “同一主模型通过控制器 Variant 切换”和共享 Excel 标准工况库尚未在本文件夹实现，属于五份方案中的后续架构目标，不是《项目推进》阶段一、二当前交付已完成的内容。 |

## 当前结论与后续确认

1. 当前电脑上，阶段一正式 CSV 复跑、阶段二四工况批量和通信生命周期检查已有证据；无需将它们继续写作“待执行”。阶段二单工况默认入口现指向常规目录中的 `step_steer_python_40kmh.txt`。
2. 《项目推进》要求检查侧偏角是否得到控制，但未给出数值合格阈值。现有同条件正弦工况可用于观察改善；若需要正式 `PASS/FAIL` 性能结论，应由项目负责人确定判据，再单独评价，不得将 `execution_status=PASS` 等同于性能通过。
3. 换机使用时，在目标电脑分别确认阶段一、二的 TruckSim Run，执行“发送到 Simulink”并更新各自专用入口；先运行阶段二 `run_local_preflight`，再分别试跑单工况和批量。
4. 非零坡度、起伏场景、COM 实测、统一主模型 Variant 和共享 Excel 工况库如需纳入当前阶段，须由项目负责人另行确定范围与验收方法；未确认前仅列为扩展项。

历史 `03_数据存储`、`04_测试报告` 记录的是当时的代码与工况，不因本次代码修改而自动更新，也不应批量改写。
