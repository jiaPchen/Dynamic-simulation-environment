# 换机使用总流程

本文件用于把整个“动力学仿真环境阶段二”文件夹复制到另一台 Windows 电脑后，指导使用者完成环境配置、验证和日常运行。

阶段一、阶段二与《项目推进》及五份方案的对应关系和剩余验收项，见根目录 `01_阶段目标对照与剩余验收.md`。该清单不代替新电脑上的真实联仿验证。

本目录包含两个独立使用的项目：

| 要运行的内容 | 进入的文件夹 | 首先阅读的说明 |
|---|---|---|
| 阶段一：第一轴转向开环仿真 | `00trucksim_simulink` | `00trucksim_simulink/03_换机配置与AI交接说明.md` |
| 阶段二：Python TCP闭环联仿 | `01_trucksim_simulink_python` | `01_trucksim_simulink_python/03_换机配置与AI交接说明.md` |

一次只选择一个阶段运行。两个阶段都会使用 TruckSim 的外部数据和当前 Run，不要同时运行，也不要同时修改同一套 TruckSim 数据。

## 1. 复制前

1. 两个阶段现在均可独立复制：运行阶段一时复制完整`00trucksim_simulink`文件夹；运行阶段二时复制完整`01_trucksim_simulink_python`文件夹。若需要两个阶段，都可复制整个根目录。
2. 关闭 MATLAB、TruckSim 和正在运行的 Python 服务后再复制，避免复制到被占用或尚未写完的结果文件。
3. 直接复制本机文件夹时，仍存在的旧 `03_数据存储`、`04_测试报告` 可以一并复制用于追溯；它们包含旧电脑路径，不需要也不应批量替换。若通过 Git 获取项目，仓库只保留当前验收用的阶段一第 024 次、阶段二第 042 次和 2026-09-25 14:38 四工况批次；其余历史结果不随仓库传输。
4. 新电脑必须已安装与项目兼容的 MATLAB、TruckSim 和 Python。TruckSim 的程序目录、数据目录、solver 目录和 `simfile.sim` 不包含在本项目文件夹中，必须在新电脑本地安装或准备好。

## 2. 新电脑首次配置

### 2.1 确认软件和路径

在新电脑记录以下信息：

```text
项目根目录：
MATLAB版本：
Python完整路径（python.exe）：
TruckSim版本：
TruckSim程序目录：
TruckSim数据目录：
TruckSim solver目录（其中应有Matlab84+子目录）：
实际联仿使用的simfile.sim完整路径：
TruckSim中准备使用的Run名称：
```

然后只打开将要使用阶段中的`03_换机配置与AI交接说明.md`，按其中“必须改为本机路径的位置”逐项检查和修改。不要把旧电脑的`C:`或`D:`路径直接保留到新电脑。

阶段二需要同步检查 MATLAB 脚本中的`trucksimSimFile`与 YAML 中的`simfile_path`，二者必须是同一个文件。阶段一则主要检查`run_case_new.m`中的 Python、solver、`simfile.sim`和项目内模型路径。

### 2.2 准备TruckSim

1. 在新电脑启动 TruckSim，并导入对应阶段的`.cpar`文件：阶段一使用`00trucksim_simulink/ORAC-BLFISMC1017 #phase1.cpar`，阶段二使用`01_trucksim_simulink_python/ORAC-BLFISMC1017 #phase2.cpar`。
2. 在 TruckSim 中确认要使用的联仿 Run，并确认其 S-Function 使用的`simfile.sim`就是前一步记录的文件。
   若复制了两个阶段，另一阶段的 `runtime/*.path` 仍指向旧电脑且目标文件不存在，保存脚本只会警告该阶段尚未配置；等两个阶段都在新电脑重新保存入口后，才有完整的跨阶段 Run 隔离检查。每个阶段仍必须分别确认自己的 Run，不可仅凭旧路径记录判断。
3. 首次真实运行前，备份该`simfile.sim`及其关联的`.par`文件，并确认该 Run允许项目脚本修改。项目会按 TXT 工况配置速度、停止时间等参数。

## 3. 先验证，再运行

### 阶段一

1. 打开`00trucksim_simulink/03_换机配置与AI交接说明.md`，完成本机路径修改。
2. 在 MATLAB 中打开`00trucksim_simulink/00_simulink/new_three_axle_vehicle_2dof_3dof_Trucksim.slx`，只检查模型，不要在模型窗口直接点击运行。
3. 将 MATLAB 当前文件夹切换到`00trucksim_simulink/01_一键启动脚本`。
4. 在`run_case_new.m`中确认`caseFile`指向`02_测试用例`中的一个 TXT 文件。
5. 运行：

```matlab
run_case_new
```

6. 完成后检查`03_数据存储`中是否生成 CSV，以及`04_测试报告`中是否生成 PNG、Word 报告和`report_manifest.json`。

### 阶段二

1. 打开`01_trucksim_simulink_python/03_换机配置与AI交接说明.md`，完成本机路径修改。
2. 在`01_trucksim_simulink_python/05_python_controller`中，用目标 Python 检查依赖：

```powershell
& "<目标机python.exe>" -c "import yaml,numpy,matplotlib,docx;print('python deps ok')"
```

3. 在 MATLAB 中打开`01_trucksim_simulink_python/00_simulink/three_axle_vehicle_2dof_3dof_Trucksim_python.slx`，只检查模型，不要先直接运行。
4. 将 MATLAB 当前文件夹切换到`01_trucksim_simulink_python/01_一键启动脚本`。
5. 先运行只读预检：

```matlab
run_local_preflight
```

6. 在`run_case_python.m`中确认`caseFile`指向`02_测试用例`内的一个 TXT 文件，然后运行：

```matlab
run_case_python
```

7. 检查新建运行目录的`*_python_stdout.log`中是否出现`CONFIG_READY`、`HANDSHAKE_OK`和`SERVER_STOPPED`；再检查 CSV、曲线和 Word 报告是否生成。确认单工况通过后，才运行：

```matlab
run_batch_python
```

## 4. 日常使用规则

1. 日常只从`01_一键启动脚本`内的入口脚本启动，不直接在 Simulink 模型窗口点击运行。
2. 修改工况时，优先修改`02_测试用例`中的 TXT 文件；不要手工修改历史 CSV、历史报告或其内部旧路径。
3. 每次真实仿真都可能修改当前 TruckSim Run关联的`.par`数据。运行时不要启动另一套使用同一 Run或同一`simfile.sim`的仿真。
4. 运行失败时保留本次 CSV、MATLAB报错和`*_python_stdout.log`，不要先删除结果；这些文件是排查路径、TruckSim配置和 TCP通讯问题的依据。

## 5. 让AI协助配置时可直接发送

```text
我把“动力学仿真环境阶段二”整个文件夹复制到了新电脑，准备运行【阶段一/阶段二】。
请先阅读根目录的“00_换机使用总流程.md”和对应阶段的“03_换机配置与AI交接说明.md”。

我的电脑信息：
项目根目录：
MATLAB版本：
Python完整路径：
TruckSim版本：
TruckSim程序目录：
TruckSim数据目录：
solver目录：
simfile.sim完整路径：
准备使用的TruckSim Run：

请先只读检查，列出旧路径到新路径的修改清单和TruckSim数据写入风险；我确认后再修改项目内脚本。不要修改历史数据、报告或.slx文件；未经我确认，不要执行会修改TruckSim .par文件的真实仿真。
```
